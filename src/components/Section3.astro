---
import Card from "../components/Card.astro";
import { Image } from "astro:assets";
import beforeImg from "../assets/t000-male-35y-weight-loss-before.jpg";
import afterImg from "../assets/t000-male-35y-weight-loss-after.jpg";

const PORTRAIT_SIZES =
  "(min-width:1024px) 256px, (min-width:768px) 224px, (min-width:640px) 192px, 160px";

// Lean set: exact 1× widths + 2× picks (all ≤ your 768px original)
const PORTRAIT_WIDTHS = [160, 192, 224, 256, 320, 512];
---

<Card variant="dark" title="The Science">
  <!-- Content Grid -->
  <div class="grid items-center gap-10 md:grid-cols-2">
    <!-- Images Card -->
    <div class="mb-20 flex flex-col items-center rounded-xl bg-white p-8">
      <div class="flex gap-4">
        <!-- BEFORE -->
        <figure class="flex flex-col items-center">
          <Image
            src={beforeImg}
            alt="Before — week 0"
            sizes={PORTRAIT_SIZES}
            widths={PORTRAIT_WIDTHS}
            class="h-auto w-40 rounded-md sm:w-48 md:w-56 lg:w-64"
            loading="lazy"
          />
          <figcaption
            class="font-semibold tracking-wide text-black/80 uppercase"
          >
            Before
          </figcaption>
        </figure>

        <!-- AFTER -->
        <figure class="flex flex-col items-center">
          <Image
            src={afterImg}
            alt="Before — week 0"
            sizes={PORTRAIT_SIZES}
            widths={PORTRAIT_WIDTHS}
            class="h-auto w-40 rounded-md sm:w-48 md:w-56 lg:w-64"
            loading="lazy"
          />
          <figcaption
            class="font-semibold tracking-wide text-black/80 uppercase"
          >
            After
          </figcaption>
        </figure>
      </div>

      <p class="mt-6 max-w-xs text-center text-xs text-neutral-500">
        Results may vary depending on your condition, starting weight, and
        commitment to the program.
      </p>
    </div>

    <!-- Text Content -->
    <div>
      <h2
        class="mb-4 text-3xl leading-tight font-extrabold text-white md:text-4xl"
      >
        AESTHETIC OPTIMIZATION
      </h2>
      <h3 class="mb-2 text-lg font-bold text-white uppercase">
        With <span class="text-cyan-400">Proven Scientific Methodology</span>
      </h3>
      <div class="space-y-3 text-base leading-relaxed text-white">
        <p>
          You could get fit for free. Information isn’t the problem. You’ve
          tried it on your own for years.
        </p>
        <p>
          But you’re too busy making money and providing a great life for your
          family. Doing this on your own isn’t working.
        </p>
        <p>
          And every time you go on vacation, you’re still embarrassed to take
          your shirt off. Our method guarantees that problem will be eliminated
          forever.
        </p>
      </div>
    </div>
  </div>
</Card>
