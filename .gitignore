# ─── Editor / IDE folders ────────────────────────────────
.vscode/            # VS Code workspace settings
.idea/              # JetBrains (already present, leave it)

# ─── OS side‑cars ────────────────────────────────────────
.DS_Store           # macOS (already present, keep)
Thumbs.db           # Windows thumbnail cache
*:Zone.Identifier   # Windows ADS metadata (e.g. the file you saw)

# ─── Build & tooling output ─────────────────────────────
dist/               # Astro build output  (already present)
.astro/             # Astro typegen       (already present)
coverage/           # test coverage reports (if you add Jest/Vitest)
*.log               # generic log catch‑all

# ─── Env & secrets ──────────────────────────────────────
.env*
!.env.example       # keep a sample file if you create one

# ─── Assets you don’t want in repo ──────────────────────
public/older versions/
